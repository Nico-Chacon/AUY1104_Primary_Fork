apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-api-canary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-api-canary
  template:
    metadata:
      labels:
        app: demo-api-canary
    spec:
      containers:
      - name: demo-api
        image: chaconnico/mi-api-academica:canary
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: demo-api-service
spec:
  selector:
    app: demo-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
